import React from "react";
// import { render } from "@testing-library/react";

describe('App test is running', () => {
  it('There is no need to test / App.test.tsx', () => {
    expect(2 + 2).toEqual(4)
  })
})