import React, { FC } from "react";
import KIcon, { IconProps } from "../Icon";

export const BasicIcon: FC<Partial<IconProps>> = props => {

  return (
    <>
      <KIcon icon="cab" />
    </>
  )
}

export default BasicIcon